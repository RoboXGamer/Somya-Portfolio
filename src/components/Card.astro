<div
  class="w-full h-full rounded-4xl border border-solid border-gray-300 relative"
>
  <div class="inset-0 absolute grid-background rounded-4xl">
    <div class="inset-0 absolute grid-background-2 rounded-4xl">
      <slot />
    </div>
  </div>
</div>

<style>
  .grid-background {
    --bg-color: hsl(0, 0%, 85%);
    --size: 70px;
    background-image: linear-gradient(
        to right,
        var(--bg-color) 1px,
        transparent 1px
      ),
      linear-gradient(to bottom, var(--bg-color) 1px, transparent 1px);
    background-size: var(--size) var(--size);
    background-repeat: repeat;
  }
  .grid-background::after {
    content: "";
    border: 1px solid rgb(201, 201, 201, 1);
    width: 100%;
    height: 100%;
    position: absolute;
    inset: 0;
    border-radius: inherit;
    pointer-events: none;
    box-shadow:
      inset 0 -8px 0.5px #bcbcbc4d,
      inset 0 2px 0.5px 0 var(--color-white);
  }

  .grid-background-2 {
    --bg-color: rgb(254, 254, 254);
    background-image: radial-gradient(
      circle closest-side at center,
      transparent 0%,
      transparent 30%,
      var(--bg-color) 100%
    );
  }
</style>
